# 工程经验库 · 总目录

> 说明：正文均脱离原项目，可独立阅读；来源证据见各文档附录。  
> 来源项目：gbrain v0.42.56.0（代码地图见 [`../../gbrain-代码地图.md`](../../gbrain-代码地图.md)）

## 架构层（整体怎么搭、为什么这么选）

- [用统一操作契约为 CLI 与 Agent 工具提供单一真相源](架构/用统一操作契约为CLI与Agent工具提供单一真相源.md) —— 100 个 Operation 一处定义，CLI + MCP 双通道生成，避免接口漂移
- [用正交双轴模型分离「数据库」与「内容仓库」路由](架构/用正交双轴模型分离数据库与内容仓库路由.md) —— Brain（哪库）× Source（库内哪 repo）独立解析，防 silent misroute

## 方案层（某类问题的完整解法）

### 安全 / 信任边界

- [远程调用 Fail-Closed 信任边界](方案/安全/远程调用Fail-Closed信任边界.md) —— `remote` 必填、默认 untrusted、读侧 scope 统一 ladder
  - [阶段1-读侧数据源作用域解析](方案/安全/远程调用Fail-Closed信任边界/阶段1-读侧数据源作用域解析.md) —— federated 数组 > 标量 > 空 的三级 precedence
  - [阶段2-protected-job-name的trust判别用remote而非scope](方案/安全/远程调用Fail-Closed信任边界/阶段2-protected-job-name的trust判别用remote而非scope.md) —— op admin scope 之外叠加 `remote !== false` 二次校验 + queue.add 第四层冗余，protected job 名 fail-closed

### 检索 / 缓存

- [命名能力包与版本化缓存指纹防污染](方案/检索/命名能力包与版本化缓存指纹防污染.md) —— 安装时选 mode bundle，缓存键 fold 全量 knobs + 版本号，防 cross-mode 串读
  - [阶段1-计算KnobsHash缓存键](方案/检索/命名能力包与版本化缓存指纹防污染/阶段1-计算KnobsHash缓存键.md) —— 33 字段固定顺序拼接 + SHA256 截 16 位 hex

### 检索 / 排序融合

- [加权倒数排名融合合并多路召回结果](方案/检索/加权倒数排名融合合并多路召回结果.md) —— 多路排名用 1/(k+rank) 投票累加，意图通过调 k 而非乘分数倾斜
  - [阶段1-RRF得分计算与归一化](方案/检索/加权倒数排名融合合并多路召回结果/阶段1-RRF得分计算与归一化.md) —— rank 从 0 起、跨路累加、÷maxScore 归一化、compiled_truth ×2.0 boost
  - [阶段2-意图感知的每路有效k调整](方案/检索/加权倒数排名融合合并多路召回结果/阶段2-意图感知的每路有效k调整.md) —— k_eff = k_base/weight，entity/event 偏关键词，temporal 靠 recency 不调 k

### 检索 / 容错降级

- [检索后处理阶段的 fail-open 姿态](方案/检索/检索后处理阶段的fail-open姿态.md) —— 后处理阶段失败返回输入不变 + 审计行，检索链路永不因后处理中断；与 Fail-Closed 信任边界形成对照
  - [阶段1-rerank双层try-catch兜住业务错与审计写错](方案/检索/检索后处理阶段的fail-open姿态/阶段1-rerank双层try-catch兜住业务错与审计写错.md) —— 外层 catch 兜业务错返回 RRF 原序，内层 catch 兜审计写错静默吞，降级严格"输入不变"且 rerank_score 未设置让 autocut 级联 noOp
  - [阶段2-cosineReScore静默不写审计的缺口](方案/检索/检索后处理阶段的fail-open姿态/阶段2-cosineReScore静默不写审计的缺口.md) —— 单层 try/catch 静默吞 DB 错不写审计，与阶段1形成姿态不一致的对照缺口，降级不可观测
  - [阶段3-autocut多重no-op守卫与rerank fail-open级联](方案/检索/检索后处理阶段的fail-open姿态/阶段3-autocut多重no-op守卫与rerank fail-open级联.md) —— 无 try/catch 靠多重 no-op 守卫降级，与依赖类型匹配的合理姿态；隐式契约级联 rerank fail-open（rerank_score 未设置→scoreOf 取 undefined→守卫二触发）

### 运维 / 分布式锁

- [DB 锁的心跳续期与 steal grace 设计](方案/运维/DB锁的心跳续期与steal-grace设计.md) —— TTL 兜底 + 6x 余量心跳 + heartbeat-aware steal grace + direct pool，四要素联动防 holder 崩溃/卡顿/池耗尽三种失效
  - [阶段1-心跳间隔TTL除以6的6x余量](方案/运维/DB锁的心跳续期与steal-grace设计/阶段1-心跳间隔TTL除以6的6x余量.md) —— refreshIntervalMs = max(15s, TTL/6)，6x 余量让漏 1 个 tick 不过期，与 steal grace = 2× 间隔联动

## 状态速览

| 状态 | 数量 |
|------|------|
| draft | 11 |
| reviewed | 5 |
| applied | 0 |
| deprecated | 0 |

## 下一步计划（来自代码地图 P0）

- [~] 混合检索全链路（RRF + relational recall + rerank fail-open）—— RRF 融合 + rerank/cosineReScore/autocut fail-open 三姿态对照已完成，剩 relational recall
- [~] Sync 可恢复导入（lock + checkpoint + pace）—— per-source lock 心跳续期方案已完成，剩 checkpoint / pace / stall watchdog
- [ ] Dream cycle 阶段机与 protected handlers —— 深读笔记已就绪
- [ ] Minions 作业调度与锁续期
